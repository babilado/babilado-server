[package]
authors = ["babilado developers"]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "babilado-server"
version = "0.1.0"

[dependencies]
anyhow = "1.0"
babilado-types = "0.2.0"
jsonl = {version = "4.0", features = ["tokio"]}
tokio = {version = "1.0", features = ["full"]}
